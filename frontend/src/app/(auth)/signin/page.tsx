import { SigninForm } from "@/components/forms/signin-form";

/**
 * A functional component representing the sign-in page.
 *
 * This component renders the SigninForm, allowing users to enter their
 * credentials to sign in.
 *
 * @returns A JSX element representing the sign-in page.
 */
const SignInPage = () => {
  return <SigninForm />;
};

export default SignInPage;
